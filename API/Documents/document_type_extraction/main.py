

def generate_metadata(): 
    # attempt with default method
    # default_attempt = default_identification(file)
    # if default_attempt:
    #   we've identified the document type from a default method without issue
    #   return default_attempt 
    # else:
    #   try to identify the document type using rag 
    #   rag_attempt = rag_identification(file)
    #   if rag_attempt:
    #     we've identified the document type from the rag method without issue
    #     return rag_attempt